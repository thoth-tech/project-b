#pragma once
#include "splashkit.h"

extern bitmap background;
extern bitmap bee;
extern bitmap box;
extern bitmap bullet;  // Keep your bullet bitmap
extern float player_posx;
extern float player_posy;
extern int RIGHT_BOUNDARY;
extern int LEFT_BOUNDARY;
extern int GRAVITY;
extern int WINDOW_WIDTH;  // Add from upstream
extern int WINDOW_HEIGHT; // Add from upstream
extern int spawn_interval;
extern float BEE_SCALE;   // Add from upstream
